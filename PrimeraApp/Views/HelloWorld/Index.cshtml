@{
    ViewBag.Title = "Administrador";
}
<h2>@ViewBag.Title <button type="button" class="btn btn-default">Nueva Categoria</button></h2>
<h3>@ViewBag.Message</h3>

<h2>Catalogo</h2>

<div class="row">
    <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
            <img src="@Url.Content("~/Content/img/rascador_tubo_con_juguete_para_gatos.jpg")" />
            <div class="caption">
                <h3>Juguetes</h3>
                <p><a href="#" class="btn btn-primary" role="button">Button</a> <a href="#" class="btn btn-default" role="button">Button</a></p>
            </div>
        </div>
    </div>


    <div class="col-sm-6 col-md-4">
        <div class="thumbnail">
            <img src="@Url.Content("~/Content/img/Don_Cucho.jpg")" style="max-width:245px;max-height:450px" />
            <div class="caption">
                <h3>Alimentos</h3>
                <p><a href="#" class="btn btn-primary" role="button">Button</a> <a href="#" class="btn btn-default" role="button">Button</a></p>
            </div>
        </div>
    </div>
</div>
<p>Aqui hay que poner el crear categorias y toda la cosa</p>
<!--
<div class="card-body">
    <h1 class="card-title">
        {$asunto}
        <a href="#" class="btn btn-outline-secondary">Nueva Categoria</a>
    </h1>
    {include file="../partials/_messages.tpl"}
    {if isset($categorias) && count($categorias)}
    <div class="row row-cols-1 row-cols-md-3 g-4">
        {foreach from=$categorias item=model}
        <div class="col">
            <div class="card h-100">
                modificar aca para poner una nueva imagen para la categoria
        {html_image file="#" height=100px width=100px}-
                    <div class="card-body">
                        <h5 class="card-title">{$model.nombre}</h5>
                    </div>
                    <div class="card-footer">
                        <a href="{$_layoutParams.root}categorias/show/{$model.id}" class="btn btn-sm btn-success">Ver</a>
                        <a href="{$_layoutParams.root}categorias/edit/{$model.id}" class="btn btn-warning btn-sm">Editar</a>
                    </div>
                </div>
            </div>
            {/foreach}
        </div>
        {else}
        <p class="text-info">{$notice}</p>
        {/if}

    </div>->
